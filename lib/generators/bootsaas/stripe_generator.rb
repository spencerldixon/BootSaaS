require 'rails/generators'
module Bootsaas
  class StripeGenerator < Rails::Generators::Base
    def install
      #Install gems
      #Install stripe-event
      #Add JS
      #Add service objects
      #Push all to background worker?
      #Do you want to install to bootsaas templates?
      # - Adjust templates to include JS
      # - Add to devise forms
      # - Add to edit profile thing
      # - Add a 'my subscriptions' controller? Or just add it to edit profile?
    end
  end
end
